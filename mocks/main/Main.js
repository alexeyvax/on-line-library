/*import React from 'react';
import { mount } from 'enzyme';

import Main from '../../../components/main/Main.jsx';
import data from '../../../../mocks/listBooks';

describe('Main', () => {
	const searchIsEmpty = true;
	const classNameComponent = 'list-books';
	const output = mount(
		<Main
			data={data}
			searchIsEmpty={searchIsEmpty}
		/>
	);
	
	it('renders correctly Main', () => {
		expect(output.hasClass(classNameComponent)).toBeTruthy();
		const book = output.find('.book');
		// console.log(book);
		// (book.text()).toBe('name test');
		// data.forEach(({ book }, i) => expect(span.at(i).text()).toBe(book));
	});
});*/
